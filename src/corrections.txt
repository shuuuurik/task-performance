1. В файле index.html добавлены атрибуты:
     async для подключаемого скрипта "stats.js", 
     defer для подключаемого скрипта "contacts.js"

2. В файле contacts.js:

    2.1. 
    Добавлена глобальная переменная items:
    "const items = contacts.getElementsByClassName("contact");"
    - динамическое хранилище элементов списка (не нужно получать каждый раз в обработчике скролла)

    2.2.
    Изменен обработчик скролла: теперь индекс элемента в заголовке определяется с учетом высоты элемента списка
    (длину скролла делим на высоту одного элемента списка)
    "const topItemIndex = Math.ceil(contacts.scrollTop / 18);
    stickyHeader.textContent = items[topItemIndex].textContent;"

    2.3. 
    Изменены функции addContacts() и дополнен обработчик скролла: теперь загрузка страницы работает с подгрузкой 
    - сперва отрисовываются первые 5000 элементов списка; после прокрутки до конца текущего списка появляются следующие 5000 
    элементов и так далее до 50000 (используются два указателя l и r - глобальные переменные).
